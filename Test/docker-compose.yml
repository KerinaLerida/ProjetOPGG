version: '3'

services:
  mongodb:
    build:
      context: ../MongoDB
    ports:
        - "27017:27017"

  scrapy:
    build:
      context: ../Scrapy
    depends_on:
      - mongodb

  application:
    build:
      context: ../Application
    ports:
    depends_on:
      - mongodb
      - scrapy
